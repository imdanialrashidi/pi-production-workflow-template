---
description: Implement one accepted bounded vertical slice end to end
argument-hint: "<task>"
---

Implement this accepted task:

$ARGUMENTS

Follow `AGENTS.md` and the project execution policy.

1. State the acceptance target, scope, and non-goals briefly.
2. Use `scout` automatically when discovery is genuinely unclear.
3. Implement the smallest coherent vertical slice with risk-based tests.
4. Use `verification-routing` and the narrowest reliable checks during implementation.
5. Use `reviewer` automatically for meaningful high-risk or multi-module work.
6. Resolve confirmed findings, inspect the final diff, and report exact evidence.

Do not add adjacent features, speculative abstractions, deployments, commits, pushes, or unrelated refactors.
