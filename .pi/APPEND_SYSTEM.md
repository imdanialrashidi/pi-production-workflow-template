# Project execution policy

Use `AGENTS.md` as the task map; load only the procedure needed for the current decision. Follow `docs/GIT_POLICY.md` for scoped automatic PR handoff; read-only/local-only tasks and evals never publish.

If the same approach fails twice without new evidence, use the recovery ladder in `docs/HARNESS.md`. Complete unblocked work and report only observed evidence.
